import React from "react";

const Logo = () => {
  return (
    <picture>
      <h1 className="text-[clamp(1.2rem,4vw,2.5rem)] font-bold uppercase">
        Farm Logo
      </h1>
    </picture>
  );
};

export default Logo;
