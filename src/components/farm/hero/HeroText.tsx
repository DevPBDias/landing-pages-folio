import React from "react";

const HeroText = () => {
  return (
    <section>
      <p className="font-medium text-[clamp(1rem,4vw,1.6rem)]">
        Original & Natural
      </p>
      <h1 className="font-bold text-[clamp(1.8rem,4vw,4rem)] uppercase text-[var(--secondary-color)]">
        Agriculture matters
      </h1>
      <h3 className="font-medium text-[clamp(1.5rem,4vw,3rem)] mb-2">
        Good production
      </h3>
      <p className="text-[clamp(0.8rem,4vw,1.3rem)]">
        Dissuade ecstatic and properly saw entirely sir why laughter endeavor.
        In on my jointure horrible margaret suitable he speedily.
      </p>
    </section>
  );
};

export default HeroText;
